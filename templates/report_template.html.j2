<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Health Report for {{ worker_name }}</title>
    <style>
        body { font-family: sans-serif; color: #333; line-height: 1.6; }
        .container { margin: 0 auto; padding: 30px; max-width: 800px; }
        h1 { color: #0056b3; border-bottom: 2px solid #0056b3; padding-bottom: 10px; }
        h2 { color: #333; margin-top: 30px; border-bottom: 1px solid #ccc; padding-bottom: 5px; }
        .report-content { white-space: pre-wrap; /* This respects newlines from the AI's response */ }
        .footer { text-align: center; margin-top: 40px; font-size: 0.8em; color: #777; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Health Risk Report for {{ worker_name }}</h1>
        <div class="report-content">{{ report_content }}</div>
        <div class="footer">
            <p>This AI-generated report is for informational purposes only and is not a substitute for professional medical advice.</p>
        </div>
    </div>
</body>
</html>